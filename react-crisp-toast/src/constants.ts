import { ToastPosition, ToastType } from "@src/context/ToastContext";

export const DEFAULT_DURATION = 5000;
export const DEFAULT_POSITION: ToastPosition = {
  vertical: "top",
  horizontal: "right",
};
export const DEFAULT_TYPE: ToastType = "info";
export const DEFAULT_THRESHOLD = 150;
export const DEFAULT_FADE_OUT_DURATION = 500;
